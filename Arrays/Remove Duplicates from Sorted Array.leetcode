class Solution {
    public int removeDuplicates(int[]arr) {
        
         int n = arr.length;
         int k= 0;

         for( int i= 1; i< n; i++) {
            if(arr[i] != arr[k]) {
                k++;
                arr[k]= arr[i];
            }
         }

         return k+1;
    }
}